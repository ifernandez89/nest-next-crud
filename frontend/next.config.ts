import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.frandroid.com','servicelshop.com.mx','images.fravega.com','http2.mlstatic.com','www.megatone.net'], // Agrega el dominio aquí
  },
};

export default nextConfig;
